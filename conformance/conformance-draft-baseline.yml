# MCP Conformance Draft-Track Baseline
#
# Expected failures for the DRAFT track only: the `draft` suite of the
# pinned 0.2.0-alpha conformance tool (npm alias "conformance-draft" in
# package.json), which carries the 2026-07-28 draft-spec scenarios.
# The stable track keeps its own baseline in conformance-baseline.yml —
# the two files are tied to different installed tool versions and are
# curated independently (see docs/v2-development-plan.md, WS7).
#
# Until the v2 stateless core lands (WS1-WS3 in the development plan),
# draft-only scenarios are expected to fail: the SDK on `main` still
# implements the 2025-11-25 lifecycle. Each entry below names the
# workstream that will make it pass; entries only shrink as workstreams
# complete. The no-shortcut rule in conformance/README.md applies to this
# file exactly as it does to the stable baseline.
#
# Last updated: 2026-06-12 (WS1 milestone)
# Conformance tool version: 0.2.0-alpha.2 (draft pin)

server:
  # --- WS2 (negotiation; WS1 types half complete) ---
  # WS1 delivered the types and server/discover: the _meta envelope is
  # validated (-32602 per missing field), unsupported versions get -32004
  # with supported/requested data, DiscoverResult carries
  # supportedVersions/capabilities/serverInfo + resultType/ttlMs/cacheScope,
  # and SEP-2164/SEP-2549 behavior is implemented behind version gating —
  # all unit-tested at the session layer (tests/Server/ServerDiscoverTest,
  # HttpDiscoverTest, ModernResultAdaptationTest).
  #
  # These scenarios still fail with one shared root cause: the draft
  # tool's stateless connector sends MCP-Protocol-Version: DRAFT-2026-v1
  # (the RC's draft identifier) with the matching _meta on EVERY request,
  # and the transport's header validation rejects unknown versions with 400
  # before the session sees the request (server/discover itself is exempt
  # and answers correctly). Accepting the draft identifier and selecting
  # the modern era per-request (instead of per-session) is exactly WS2's
  # detection milestone, so these are re-attributed to WS2.
  #
  - server-stateless
    # Remaining checks additionally need per-request era detection, the
    # 400/404 HTTP status mapping for the general modern path (discover
    # already maps its errors to 400), the removed-method 404s, and
    # subscriptions/listen (WS2/WS3).

  - sep-2164-resource-not-found
    # State as of 2026-06-12: the scenario's only hard check (MUST NOT
    # return empty contents for a missing resource) PASSES — the
    # per-scenario summary shows "1 passed, 0 failed" — but its two
    # SHOULD checks (-32602 error code, data.uri) report WARNINGS because
    # the stateless connector's resources/read still 400s at the
    # version-header gate, and tool 0.2.0-alpha.2's baseline gate counts a
    # warned scenario as failing (removing this entry yields "Unexpected
    # failures"). The SDK behavior itself is implemented and unit-tested
    # (tests/Server/ModernResultAdaptationTest); the warnings clear when
    # WS2's per-request era detection lands, and this entry leaves the
    # baseline then.

  - caching
    # ttlMs/cacheScope implemented (required-on-modern, stamped defaults,
    # stripped for legacy) and unit-tested; scenario needs the stateless
    # request path (WS2) to exercise it.

  # --- WS3 (transport changes: SEP-2243 request-metadata headers) ---
  - http-header-validation
    # Mcp-Method/Mcp-Name header validation and HeaderMismatch (-32001)
    # rejection not yet implemented.

  - http-custom-header-server-validation
    # x-mcp-header schema annotation / Mcp-Param-* validation not yet
    # implemented.

  # --- WS3 (transport changes: SEP-2322 InputRequiredResult) ---
  # The multi-round-trip input mechanism replacing server-initiated
  # sampling/elicitation/roots requests is not yet implemented; the
  # existing suspend/resume pattern still uses server-initiated requests.
  - input-required-result-basic-elicitation
  - input-required-result-basic-sampling
  - input-required-result-basic-list-roots
  - input-required-result-request-state
  - input-required-result-multiple-input-requests
  - input-required-result-multi-round
  - input-required-result-missing-input-response
  - input-required-result-non-tool-request
  - input-required-result-result-type
  - input-required-result-tampered-state
  - input-required-result-capability-check
  - input-required-result-ignore-extra-params

client:
  # (WS1's json-schema-ref-no-deref entry removed 2026-06-12: the client
  # passes 2020-12 schemas through verbatim and never dereferences network
  # $refs — scenario passing.)

  # --- WS3 (transport changes: SEP-2243 request-metadata headers) ---
  - request-metadata
    # Mcp-Method/Mcp-Name header emission not yet implemented.

  - http-custom-headers
    # Mcp-Param-* mirroring of designated tool parameters not yet
    # implemented.

  - http-invalid-tool-headers
    # Client-side rejection rules for invalid x-mcp-header mappings not
    # yet implemented.

  # --- WS3 (transport changes: SEP-2322 InputRequiredResult) ---
  - sep-2322-client-request-state
    # Client side of the multi-round-trip input mechanism (inputResponses,
    # requestState round-tripping) not yet implemented.

  # --- WS3 (authorization hardening) ---
  - auth/iss-supported
  - auth/iss-not-advertised
  - auth/iss-supported-missing
  - auth/iss-wrong-issuer
  - auth/iss-unexpected
  - auth/iss-normalized
    # SEP-2468 `iss` parameter validation (RFC 9207) not yet implemented;
    # one to two checks fail per iss scenario.

  - auth/authorization-server-migration
    # SEP-2352 credential binding to the issuing server's issuer not yet
    # implemented.

  - auth/offline-access-not-supported
    # SEP-2207 refresh-token clarification: one check fails at the draft
    # version (refresh behavior when offline_access is unsupported).
